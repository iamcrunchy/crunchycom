name: Build and Deploy CrunchyCom

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false
          
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PURPLE_GROUND_0C419510F }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/frontend"
          api_location: "backend/crunchycom/" 
          output_location: "dist/frontend/browser"
          ###### KEY ADDITIONS ######
          # dotnet_version: "8.0"
          # skip_api_build: false

#
#      - name: Build And Deploy
#        id: builddeploy
#        uses: Azure/static-web-apps-deploy@v1
#        with:
#          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PURPLE_GROUND_0C419510F }}
#          repo_token: ${{ secrets.GITHUB_TOKEN }}
#          action: "upload"
#          
#          ###### Monorepo Configuration ######
#          app_location: "/frontend"       # Where your Angular code lives
#          api_location: "/backend"        # Where your C# code lives
#          output_location: "dist/frontend/browser" # Where Angular builds to (check your dist folder name)
#          #####################################
#          
#          ### NEW HINT FOR ORYX to use the correct .net version ###
#          dotnet-version: "8.0"

#name: Build and Deploy CrunchyCom
#on:
#  push:
#    branches: [ "main" ]
#    paths:
#      - 'backend/**'
#      - 'frontend/**'
#      - '.github/workflows/main_deploy.yml'
#  pull_request:
#    branches: [ "main" ]
#
#jobs:
#  # --- BACKEND JOB ---
#  build-backend:
#    runs-on: ubuntu-latest
#    defaults:
#      run:
#        working-directory: ./backend
#
#    steps:
#      - uses: actions/checkout@v4
#
#      - name: Setup .NET
#        uses: actions/setup-dotnet@v4
#        with:
#          dotnet-version: '8.0.x'
#
#      - name: Restore dependencies
#        run: dotnet restore
#
#      - name: Build
#        run: dotnet build --configuration Release --no-restore
#
#      # add test step later when we have unit tests
#      # - name: Test
#      # - run: dotnet test --no-build --verbosity normal
#
##--- FRONTEND JOB ---
#build-frontend:
#  runs-on: ubuntu-latest
#  defaults:
#    run:
#      working-directory: ./frontend
#
#  steps:
#    - uses: actions/checkout@v4
#
#    - name: Setup Node.js
#      uses: actions/setup-node@v4
#      with:
#        node-version: '22.0.x'
#
#    - name: Install dependencies
#      run: npm ci
#
#    - name: Build Angular
#      run: npm run build --configuration=production
#
#    # Optional: upload build artifacts if you aren't deploying to Azure yet
#    - name: Upload Build Artifact
#      uses: actions/upload-artifact@v4
#      with:
#        name: frontend-dist
#        path: ./frontend/dist
#